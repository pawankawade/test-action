name: Test

on:

  push:

    branches:

      - main

jobs:

  test:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v3

    - name: Install dependencies
      with:
        node-version: 18
      
      run: npm install

    - name: Run tests

      run: npm test
